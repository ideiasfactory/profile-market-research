# Professional Profile Analyser — Open WebUI Python tool

Paste [`ppa_tools.py`](./ppa_tools.py) into **Espaço de Trabalho → Ferramentas** when the preferred OpenAPI Global Tool Server path is unavailable.

Prefer: **Configurações → Admin → Integrações** + URL `http://host.docker.internal:8000` / Path `openapi-gpt-slim.json` (see parent README).

Valves defaults assume Open WebUI in Docker (`ppa_base_url=http://host.docker.internal:8000`).
