# ADR-009 — JSONL for MVP Persistence

Decision: v1 stores research history and observations in JSONL files for the MVP.

Reason: JSONL is simple, append-friendly and sufficient before introducing PostgreSQL.
